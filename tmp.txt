Add behaviour tests to cover:
- generics on classes, not just interfaces
- `ab: A | B` where A and B only appear in the union, nowhere else in the file
- union with undefined and any
- union lists `(A|B)[]`
- complex types in generics `A<B|C>`, `A<B|null>`, `A<B[]>`
- anonymous object types, shallow and deep, with and without imported types, types imported from source file and external
  - also test object type is copied correctly to generated file
- immutable class with generics
  - simple and complex generics as for types,
  - extends `A<B extends C>` and `Property<Type, Key extends keyof Type>`

In addition, review the tests and look for more edge cases like these that are not covered



Use subagents to search "tests/*/*.test.ts" for tests that use regular expressions to test for the inclusion of import using `toMatch`. Update these tests so that:
- one test per imported file
- uses `toContain` with the expected import
  - imports are always in natural order

`toMatch` should only be used for exclusion tests (does not import). Do not modify exclusion tests.

- Do not regenerate any files.
- Start with the "*.test.ts" file to determine usages of `toMatch`. Ignore cases of `not.toMatch`
  - **Incorrect** `expect(generated).toMatch(/import type \{[^}]*\bCoord\b[^}]*\} from '\.\/coord\.js'/);`
  - **Correct** `expect(generated).toContain("import type { Coord } from './coord.js';");`
  - **Incorrect** `expect(generated).toMatch(/import type \{[^}]*\bA\b[^}]*\} from '\.\/ab\.js'/);` and `expect(generated).toMatch(/import type \{[^}]*\bB\b[^}]*\} from '\.\/ab\.js'/);`
  - **Correct** `expect(generated).toContain("import type { A, B } from './ab.js';");`




Use subagents to review the tests in ./tests looking for
- missing import tests
- tests that are only test primitive case where there could be a test using in imported class, type or interface
  - do not search outside the given test

Do not regenerate files. Do not run tests.

Output each review to its own temporary file. Do no design or implement fixes, just note coverage failure

After all reviews are complete, cross check the results to eliminate false positives and compile the final results so they can be used to design new tests

Does this require any clarification?
